# حالة مشروع ماجد كرف — التوثيق الشامل

**آخر تحديث:** 2026-09-04 (✅ منطق W3 مُثبَت طرفاً لطرف · العائق الوحيد: قناة واتساب) · Location `jo8GkEPOGeRVWN5khioH`
**كل الأرقام هنا مقروءة حيّاً من GHL API — ليست منقولة من تقارير سابقة.**

---

## 1. اللوحة السريعة

| الطبقة | الإنجاز | الحالة |
|---|---|---|
| نموذج البيانات والحقول | **100%** | ✅ 14/14 حقل مطابقة مؤكّد |
| جسر المناطق (`district_slug`) | **99%** | ✅ 424/429 مشروع |
| محدّث الأسعار + الكرون | **100%** | ✅ شغّال · ✅ خلل الـ61 أُصلح **ومُثبَت بكرون 2026-09-02** |
| محرّك المطابقة (Cloudflare) | **95%** | ✅ MATCHED حقيقي مثبّت |
| **الـWorkflows (W1–W6)** | **~55%** | ✅ W2 draft · ✅ **W3 — المجموعة الأولى تعمل ومُثبتة طرفاً لطرف** (draft) · 🔴 السلسلة ×8 · 🔴 W4–W6 غير موجودة |
| ربط بوت الاستقبال | **0%** | 🔴 |
| تغطية أسعار USD | **28%** | 🟠 121/429 |

**الخلاصة:** البنية التحتية جاهزة ومُثبتة عملياً. طبقة الأتمتة هي العائق الوحيد المتبقّي.

> ✅ **مانع `DRY_RUN` سقط (2026-09-03)** — لا وجود لأي مفتاح وضع تجريبي. التفصيل في §4.
>
> ✅ **منطق W3 مكتمل ومُثبَت بتشغيلين حقيقيين (2026-09-04 · 12:14 و12:24)** — تعبئة فورية بلا `Wait` · بيانات طازجة · الاستبعاد يعمل · الـAssociation تُنشأ.
>
> ✅ **الفاصلة البادئة بريئة نهائياً** — كانت حاضرة والمحرّك استبعد صحيحاً. **العقدة ⑥ لا تُلمس.**
>
> 🔴 **العائق الوحيد المتبقّي: قناة واتساب غير موصولة** — `Error 101: No connected instances found`. الرسائل تُبنى صحيحة ولا تصل. **خارج W3 — إعداد قناة لا أتمتة.**

---

## 1-ب. أول تشغيل حقيقي لـW3 — 2026-09-04

**الفرصة `63oh6ELZtHHN6AUsY2jX` · نُقلت إلى `Active — Nurture Running` 03:56:02 · انتهى الـworkflow 03:56:22 (20 ثانية).**

### ✅ ما نجح

**سلسلة العقد تعمل كما صُمِّمت:** القفل رُفع · حارس المرحلة سمح · الويبهوك نُفِّذ · الـWait مرّ · حارس المطابقة فرّع. **العطل في البيانات لا في المخطط.**

كما حُسم **فحص التكرار**: عقدة `Match Guard` واحدة لا اثنتان — القاعدة المسجّلة (Hard Refresh قبل أي إعادة بناء) منعت حارساً شبحياً.

### 🔴 عطل مؤكّد — القفل يبقى مرفوعاً على فرع NO

`Nurture Active = Yes` بعد انتهاء الـworkflow. الفرع NO يذهب إلى `END` بلا `Unlock`.

**والـW3 منشور:** كل فرصة تدخل `Active — Nurture Running` تُقفَل نهائياً و`L1` يرسلها إلى END في كل محاولة لاحقة. **و73% من المخزون خارج المطابقة، ففرع NO هو المسار الأكثر استعمالاً.**

**✅ أُمِّن (2026-09-04):** W3 أُعيد إلى **Draft** · و`Nurture Active` ضُبط على `No` على فرصة الاختبار.
**يبقى التوصيل نفسه:** فرع NO ← `Unlock — Set Nurture Active = No` قبل أي نشر.

### 🎯 السبب — حُسم بقراءة سجل الـEnrollment

**استجابة الويبهوك: `200` · `MATCHED` · Mavera Boutique · 232,209 USD · `match_found: "Yes"` · `association_summary.created: 1`.**

**المحرّك سليم تماماً** — طابق وفلتر ورتّب وأنشأ العلاقة ورجّع كل شيء متزامناً.

**العطل:** العقدة ④ تقرأ `Match Found` من **حقول الفرصة**، و**المحرّك لا يكتبها**. الاستجابة قالت `Yes` والحقل بقي `No` → سلك فرع NO.

**وليس سباق الـ10 ثوانٍ:** الحقول رُوقبت **3 دقائق** ولم تظهر. **زيادة الـ`Wait` لا تصلح شيئاً.**

> 🔴 **يهدم اعتقاداً موثّقاً** (§2 بند 4): «كتابة المحرّك على الفرصة مؤكّدة». إمّا كتبتها عقدة في W2، أو النشرة غير الموثّقة `922430af` أزالتها. **يُقرأ من الكود.** والمحرّك يكتب على GHL فعلاً — العلاقة أُنشئت — فالقدرة موجودة والحقول المخصّصة تحديداً لا تُكتب.

### ✅ العلاج المعتمد — البديل الجذري

**القراءة من استجابة الويبهوك مباشرةً وحذف الـ`Wait`** — السباق يزول من الجذر.

⚠️ **التغليف:** `{"status":200,"data":{…}}` → المسارات `data.match_found` لا `match_found`. وخريطة العقدة المحفوظة من 31 آب شكلها مختلف ⟵ **تُلتقط الاستجابة من جديد أولاً.**

| # | التعديل على W3 (`draft` الآن) |
|---|---|
| 0 | إعادة التقاط استجابة الويبهوك على العقدة #2 |
| 1 | **حذف ③ Wait 10 seconds** |
| 2 | ④ Match Guard ← `data.match_found = Yes` من الاستجابة |
| 3 | ⑤ SMS ← الاسم/المنطقة/النوع/السعر من الاستجابة |
| 4 | ⑥ Update Opportunity ← يكتب `next_match_*` + `match_found` + يضيف على `sent_project_ids` — **صار مسؤولاً عن تعبئة حقول الفرصة بدل المحرّك** |
| 5 | **فرع NO ← `Unlock — Set Nurture Active = No`** |

### ✅ مكاسب مؤكّدة من نفس القراءة

| البند | الدليل |
|---|---|
| الاستبعاد يعمل | `remaining_unsent_count: 4` من أصل 5 |
| الـAssociation ينشئها المحرّك | `created: 1` ← لا عقدة ⑦ |
| **الفاصلة بريئة نهائياً** | 200 على تشغيل حقيقي · وفرضية «`Send test request` غير صالحة» **تأكّدت** |
| محدّث الأسعار شغّال | 232,209 اليوم مقابل 232,768 بـ2 أيلول |

### ✅ نافذة الإرسال — التعارض مغلق

القراءة الحيّة: **09:00–21:00 · الأحد→الجمعة · السبت مطفأ**. **الواقع 6 أيام؛ توثيق «7 أيام» خطأ.** قرار السبت يُتخذ لاحقاً بوعي.

---

## 2. ما تغيّر في جلسة 2026-09-03

### ✅ حُسم

| # | البند | النتيجة |
|---|---|---|
| 1 | **`DRY_RUN`** | ✅ **لا وجود له.** قُرئ `RAW BODY` في W2 وW3 — لا `dryRun` ولا `dry_run` ولا `test` ولا `mode`. الحسم النهائي من دليل ثانٍ: W2 لا يرسل مفتاحاً أيضاً، ومع ذلك أنتج مطابقة **مكتوبة** على الفرصة → **غياب المفتاح = وضع حقيقي** |
| 2 | 🔴→✅ **مفتاحان مفقودان في W3** | اكتُشف أن جسم W3 كان ينقصه `sent_project_ids` و`rejected_project_ids` (7 مفاتيح مقابل 9 في W2). **أُضيفا حرفياً من W2 وحُفظا** |
| 3 | **الاستبعاد يعمل** | اختبار بمتغيّر حقيقي: أُرسل `sent_project_ids = 6a95ca37…` (Mavera Boutique) → المحرّك رجّع **Mavera Residence**. مشروع مختلف ✅ |
| 4 | **كتابة المحرّك على الفرصة** | ✅ مؤكّدة على **التشغيل الحقيقي** (حقول 1 أيلول). Test Request لا يلتزم — سلوك سليم لا عطل |
| 5 | **الـAssociation** | **المحرّك ينشئها بنفسه** — علاقة أُنشئت `2026-09-01T00:55:17` بلا أي عقدة Association في أي workflow → **لا تُضاف عقدة ⑦** |

### 🔴 الخلل الذي كاد يمرّ

لولا فحص `RAW BODY` لبُني W3 كاملاً بلا `sent_project_ids`. الأثر كان سيكون:

```
اليوم 1 → أرخص مطابق = مشروع A → يُرسَل
اليوم 3 → أرخص مطابق = مشروع A → يُرسَل ثانيةً
… 28 يوماً من نفس المشروع
```

**مفارقة:** قفل التزامن كله (`nurture_active` + 3 عقد) بُني **لحماية `sent_project_ids`** — والحقل كان يُكتب بعناية ثم **لا يُرسَل للمحرّك أبداً**.

### ⏸️ بقي معلّقاً

| البند | الحالة |
|---|---|
| **سباق قراءة الـ10 ثوانٍ** | ⚠️ **مانع نشر** — لم يُقَس زمن كتابة المحرّك على الفرصة. يُحسم في اختبار المجموعة الأولى |
| **الفاصلة البادئة في `sent_project_ids`** | ⚠️ أول إرسال ينتج `,abc`. **يُختبر بالواجهة** (لا يحتاج كود Cloudflare) — الطريقة في §4 |
| **تنظيف سجل الاختبار** | 🔴 `Sent Project IDs` **لم يُفرَّغ** رغم ما ورد في تقرير التنفيذ — قُرئ بالـAPI ولا يزال `6a95ca3749b8afb35f2a13ca` |

> **درس مسجَّل:** تقارير التنفيذ من الأدوات الخارجية **تُتحقَّق بالـAPI**، لا تُؤخذ كما هي. ثلاث دعاوى اليوم لم تطابق السجل: تفريغ الحقل · قيمة `Next Match Name` · طبيعة الاختبار (كان Test Request لا تشغيلاً حقيقياً).

---

## 3. ما تغيّر في جلسة 2026-09-02

### ✅ أُنجز

| # | الإنجاز | التفصيل |
|---|---|---|
| 1 | **إصلاح `ghl-mcp`** | كان 401 — `Authorization` بلا بادئة `Bearer `، وURL خطأ |
| 2 | **إثبات أول MATCHED حقيقي** | Mavera Boutique · 232,768 USD · Başakşehir · 2+1 |
| 3 | **إصلاح الـ61 سجل (2099)** | كُتب `2026-08-01` · 61/61 · **ومُثبَت بكرون اليوم: 16/61 حُدّثوا** |
| 4 | **حقل `nurture_active`** | أُنشئ بالواجهة · `PoxnDg8w2iJq4NrC2eLB` · RADIO `Yes`/`No` |
| 5 | **W2 → Draft** | ✅ نُفِّذ — الحاجز «منشور بلا اختبار» انحلّ |
| 6 | **W3: القفل + الويبهوك** | 3 عقد قفل مبنية · عقدة Webhook مُعدّة بالرمز السري · `Allow Re-entry = ON` · `Stop on Response = OFF` |
| 7 | **نافذة الإرسال** | 09:00–21:00 · **7 أيام** (كانت 5 — فُعِّل السبت والأحد) |

### 🔴 اكتُشف (لم يكن معروفاً)

| # | الاكتشاف | الأثر |
|---|---|---|
| 1 | 🔴 **`DRY_RUN: true` في استجابة المحرّك** | **مانع نشر** — تفصيله في §4 |
| 2 | **استجابة المحرّك متزامنة** | يرجّع `next_match_*` و`match_found` في جسم الاستجابة فوراً → `Wait` خُفِّض 30s ← 10s |
| 3 | **W3 موجود مسبقاً** (`5e7af32d-…`) | أُنشئ 2026-09-02 05:53 ولم يكن موثّقاً — كاد يُبنى مكرّر ثانٍ |
| 4 | **نسختا W1 تكمّلان بعضهما** | v2 فيها `Property Source` · v3 فيها مسار البيانات الناقصة → **دمج لا حذف** |
| 5 | **الدومين الحقيقي `app.innovationvalues.com`** | علامة بيضاء — وليس `app.gohighlevel.com` |
| 6 | **وحدة الـWorkflows ترفض الإدخال البرمجي** | تفصيله في §9 |

### ⏸️ لم يُنفَّذ

| العمل | السبب |
|---|---|
| **حسم `DRY_RUN`** | 🔴 **الأولوية القصوى** — يحتاج فتح عقدتَي الويبهوك يدوياً |
| دمج نسختَي W1 | يحتاج جدولَي عقد كاملين |
| بقية عقد W3 | الوحدة ترفض الإدخال البرمجي — يدوي أو عبر ChatGPT |
| ربط بوت الاستقبال | — |

---

## 3. حالة الـWorkflows (قراءة حيّة)

**إجمالي الحساب: 50 workflow — 35 منشور · 15 draft**

### مسار المطابقة

| # | الاسم | الحالة | المعرّف |
|---|---|---|---|
| **W1** | Buyer Matching Entry & Validation | `draft` v2 | `d74c51f0-607d-4057-94c6-6edcfb7e49b2` 🗑️ |
| **W1** | Buyer Matching Entry & Validation | `draft` v3 | `449a025b-3ba9-4fd8-8c17-cd990a96b5e3` ✅ |
| **W2** | Project Matching | ✅ **`draft`** (أُعيد 2026-09-02) | `12312c5d-8cdf-48de-a621-6557e0a6b4f3` |
| **W3** | **Options Delivery & Nurture** | ✅ **`draft`** (أُعيد 2026-09-04) — قيد إعادة التوصيل | `5e7af32d-5378-42ef-bdb2-f95daf559e60` ✅ |
| W3b | Stock Onboarding | `draft` v2 | `a1823708-d80b-4c20-80fc-03fcbe8fad28` ⚠️ اسم مضلّل — شيء مختلف، لا يُلمس |
| **W4 · W5 · W6** | — | ❌ **غير موجودة** | — |

> ✅ **W2 أُعيد إلى Draft 2026-09-02** — قاعدة الـBlueprint استُعيدت.

### حالة بناء W3 (محدَّثة 2026-09-03)

| العنصر | الحالة |
|---|---|
| التريغر `Opportunity Stage Changed` | ✅ |
| `Allow Re-entry` | ✅ ON |
| `Stop on Response` | ✅ OFF |
| نافذة الإرسال | ✅ 09:00–21:00 · 7 أيام |
| قفل التزامن (3 عقد `nurture_active`) | ✅ مبني |
| عقدة Custom Webhook | ✅ مُعدّة بالرمز السري · **9 مفاتيح مكتملة (2026-09-03)** |
| ① حارس المرحلة (Stage Guard) | ✅ |
| ③ Wait 10 seconds | ✅ |
| ④ حارس المطابقة (Match Guard) | ✅ |
| ⑤ SMS | ✅ |
| ⑥ Update Opportunity | ✅ |

> ✅ **المجموعة الأولى مكتملة — 6 عقد · 2026-09-03 مساءً.**
| نسخ المجموعة ×8 + ربط الـ`Go To` | 🔴 غير منفَّذ |
| فرع NO → `Unlock` | 🔴 **غير موصول — عطل قفل مؤكّد بالتشغيل الحقيقي 2026-09-04 (§1-ب)** |

#### المخطط الحالي (مكتمل)

```
Pipeline Stage Changed
  → L1 — Nurture Active?
       ├─ Already Active → END                    ← بلا فكّ قفل (نسخة أخرى تحمله) ✅
       └─ None → L2 — Set Nurture Active = Yes
            → Stage Guard
                 ├─ Active → #2 Call Majed Project Matching Engine
                 │      → Wait 10 seconds
                 │      → Match Guard
                 │           ├─ YES → SMS → Update Opportunity → END
                 │           └─ NO  → END   ⚠️ مؤقت — يجب أن يصير Go To
                 └─ None → Unlock — Set Nurture Active = No → END
```

> ⚠️ **فرع NO في Match Guard ينهي الـworkflow حالياً.** التصميم النهائي: `Go To` نحو عقدة الـ`Wait` التالية. **بدونه تتوقف الرعاية عند أول NO_MATCH — و73% من المخزون خارج المطابقة، فهذا المسار هو الأكثر استعمالاً.** يُربط بعد بناء السلسلة كاملة.

> ✅ **تحسين يُعتمد:** فرع NO يمرّ عبر `Unlock — Set Nurture Active = No` بدل `End Workflow` المباشر — **يحرّر القفل عند الخروج** بدل تركه مرفوعاً.

#### 📌 درس مسجَّل — العقدة التي «لم تُحفظ» كانت محفوظة

العقدة ④ أُبلغ عنها كفاشلة (اللوحة تُغلق بلا ظهور العقدة). بعد **Hard Refresh + إعادة دخول الحساب الفرعي ظهرت** — كانت محفوظة والمخطط لم يُحدَّث.

**لو أُعيد بناؤها لصار حارسان متتاليان يقرآن نفس الشرط** — يبدو كل شيء سليماً والمسار مضاعف.

> **قاعدة تُعتمد: أي عقدة تبدو أنها لم تُحفظ ⟵ Hard Refresh وعدّ العقد قبل أي إعادة بناء.**

---

## 4. البنية التحتية

### ✅ `DRY_RUN` — محسوم ومغلق (2026-09-03)

**النتيجة:** لا يوجد أي مفتاح وضع تجريبي في جسم أي من الطلبين.

| المفتاح | W2 | W3 |
|---|---|---|
| `opportunity_id` · `budget_max` · `budget_currency` · `property_location_interest` · `property_type` · `client_goal` · `property_source` | ✅ | ✅ |
| `sent_project_ids` · `rejected_project_ids` | ✅ | 🔴 كانا مفقودين → **أُضيفا 2026-09-03** |
| `dryRun` / `dry_run` / `test` / `mode` | ❌ | ❌ |

**لماذا الغياب وحده لم يكفِ:** الفرضية الأصلية شملت احتمال أن المحرّك يعتبر **غياب** المفتاح تشغيلاً تجريبياً. الحسم جاء من W2: لا يرسل مفتاحاً أيضاً، ومع ذلك أنتج مطابقة **مكتوبة على الفرصة** (Mavera Boutique · مقروءة من الحقول). إذاً **غياب المفتاح = وضع حقيقي، مُثبَت عملياً.**

**مصدر الـ`DRY_RUN: true` القديم:** اختبار يدوي مرّر العلم صراحةً، أو نسخة أقدم من المحرّك.

### 🔴 مانع النشر المتبقّي — سباق قراءة الـ10 ثوانٍ

W3 ينادي المحرّك، ينتظر **10 ثوانٍ**، ثم يقرأ حقل `Match Found` من **الفرصة**. **لم يُقَس زمن كتابة المحرّك على الفرصة إطلاقاً.**

إن تأخّرت الكتابة عن 10 ثوانٍ، يقرأ W3 القيمة **القديمة** ويرسل المشروع السابق — أي نفس عطل التكرار الذي بُني القفل لمنعه.

**الحسم:** الخطوة 4 من اختبار المجموعة الأولى — قِس زمن الرد وثبّت الـ`Wait`.
**البديل الجذري (غير محسوم):** جعل الـ`If/Else` يقرأ من استجابة الويبهوك المحفوظة مباشرةً → يُحذف الانتظار ويزول السباق من الجذر.

### ⚠️ الفاصلة البادئة في `sent_project_ids`

العقدة ⑥ تكتب `{{opportunity.sent_project_ids}},{{opportunity.next_match_id}}` — والحقل فارغ عند أول إرسال، فالناتج `,abc`.

**يُختبر بالواجهة بلا كود Cloudflare:**

```
1. اكتب في Sent Project IDs على فرصة الاختبار:  ,6a95ca3749b8afb35f2a13ca
2. أطلق Test Request من عقدة ويبهوك W3
3. اقرأ next_match_name في Response Body
```

**النتيجة (2026-09-03): 🔴 `HTTP 400 · "Bad Request"` — لا استجابة إطلاقاً.**

الفاصلة البادئة ترفضها الحمولة. الأثر: W3 ينهار من **الإرسال الثاني** وبصمت — الحقول تبقى قديمة، وحارس المطابقة يعيد إرسال نفس المشروع 8 مرات على 28 يوماً.

**✅ المحرّك بريء (قراءة الكود 2026-09-03):** `idList` في `index.js:425` يطبّق `.filter(Boolean)` بعد التقسيم — الفراغ يُحذف سليماً. **لا يحتاج أي تعديل.**

**السبب الحقيقي: الطلب لا يصل إلى الكود.** جسم الرد كان `"Bad Request"` نصاً عادياً، بينما الـworker يرجّع JSON `{"status":"ERROR",...}` — والنص غير موجود في الملف إطلاقاً.

**الفرضية الأرجح:** حقل الدمج في RAW BODY بلا علامتَي اقتباس → `"sent_project_ids": ,abc` = JSON غير صالح → 400 قبل الوصول للمحرّك. **الفحص: قارن كتابة `sent_project_ids` بكتابة `property_type` في RAW BODY.**

⛔ **لا تُنسخ المجموعة ×8 قبل الإصلاح.**

### 🗒️ الاستجابة القديمة — ملاحظة أرشيفية

**ما ظهر سابقاً:** آخر استجابة محفوظة في عقدة الويبهوك حملت `DRY_RUN: true` (اختبار 31 آب · Bahçe Bahçeşehir · 300902).

**لماذا هذا خطر:** المحرّك يملك **وضعاً تجريبياً يرجّع نتيجة مطابقة دون أن يلتزم بها**. إن كان جسم طلب W3 يحمل مفتاح وضع تجريبي — أو إن كان المحرّك يعتبر غياب مفتاح معيّن تشغيلاً تجريبياً — فسيعمل **W3 بالوضع التجريبي إلى الأبد بعد النشر**، ويرسل رسائل واتساب حقيقية عن مطابقات **لم تُسجَّل**.

> **هذا فشل صامت:** لا خطأ، لا سجل أحمر، الرسائل تصل طبيعية. لا يُكتشف إلا من شكوى عميل بعد أسابيع.

**الفحص المطلوب (دقيقتان، يدوي):** افتح عقدة Custom Webhook في W2 ثم في W3، اقرأ `RAW BODY` في كل منهما، وقارن أسماء المفاتيح. ابحث عن `dryRun` · `dry_run` · `test` · `mode`.

**⚠️ لا يُنشر W3 قبل حسم هذا.**

### ⚠️ خريطة الحقول مبنية على استجابة قديمة

الاستجابة التي حفظ GHL شكلها من **31 آب**، بينما فرصة الاختبار نفسها تحمل نتيجة أحدث (`Mavera Boutique` · 232768). **يجب تأكيد أن شكل الاستجابة الحيّة يطابق الخريطة المحفوظة**، وإلا قُرئت الحقول فارغة.

### ✅ محرّك المطابقة `majed-project-matching`
v3.0.0 · **النسخة النشطة `922430af`** (قراءة حيّة 2026-09-03) · مطابقة على `district_slug` مع fallback نصي.

> ⚠️ **التوثيق كان يقول `da81db20`** — صارت نسخة أقدم بـ3 أيام. **نسخة نُشرت ولم تُوثَّق**؛ ما تغيّر فيها غير معروف.

> 🟡 **ثغرة كامنة — مسار Resale:** `index.js:163` يقرأ `sent_property_ids` عند `source === "resale"` — و**لا W2 ولا W3 يرسلانه**. النتيجة: قائمة الاستبعاد فارغة دائماً لعملاء إعادة البيع → تكرار مشاريع. غير عاجل ما دام المسار معطّلاً (`RESALE_SKIPPED`)، لكنه ينفجر يوم يُفعَّل.

| البند | القيمة |
|---|---|
| نقطة النهاية | `POST https://majed-project-matching.majed-karaf.workers.dev/match` |
| التفويض | `Bearer` — مخزَّن كقيمة مسمّاة `Majed Matching Webhook Secret V2` (لا يظهر نصّاً) |
| نوع المحتوى | `application/json` |
| رمز الحالة | `200` |
| **شكل الاستجابة** | **متزامن** — يرجّع `next_match_*` و`match_found` في جسم الاستجابة مباشرة |

**دليل النجاح — أول MATCHED موثّق:**

| الحقل | القيمة |
|---|---|
| الفرصة | `TEST — W2 Esenyurt 2+1` (`63oh6ELZtHHN6AUsY2jX`) |
| `match_found` | **Yes** |
| `next_match_name` | **Mavera Boutique** |
| `next_match_price` | **232,768 USD** |
| المنطقة / النوع | Başakşehir / 2+1 |
| الميزانية | 500,000 USD |

المحرّك استقبل تريغر حقيقي، طابق جغرافياً، فلتر بالسعر، وكتب النتيجة. **يعمل.**

### ✅ محدّث الأسعار `majed-price-updater` — الكرون مؤكّد

**كيف تأكّدنا بلا Cloudflare:** فحصنا **النتيجة** بدل الإعداد.

| الدليل | القيمة |
|---|---|
| 40 مشروع بتاريخ | `2026-09-01T06:00:45Z` ← **6:00 صباح اليوم** |
| مطابق لـ | `0 6 * * 1-5` ✅ |
| سعر صرف جديد | `48.1165` (مقابل `48.08985` بـ31 آب) |

**الكرون محفوظ وشغّال، وسقف الـ40/دورة يعمل.**

### ✅ خلل الـ61 (تاريخ 2099) — أُصلح **ومُثبَت عملياً**

**الخلل كان:** 61 مشروعاً بـ`usd_price_updated_at = 2099-01-01` (حارس sentinel من التشغيل اليدوي). قاعدة «حدّث ما مضى عليه 48 ساعة» لا تعتبرها قديمة أبداً → متجمّدة على صرف `48.08985` للأبد.

**الإصلاح (2026-09-02 · 04:57):** كُتب `2026-08-01T00:00:00.000Z` على 61/61 — تاريخ ماضٍ حقيقي بدل تفريغ الحقل، لأن التفريغ كان سينقلهم إلى فئة الـ303 الفاضية مجهولة السلوك.

#### 🎯 الاختبار الحاسم — نجح

كرون اليوم (`2026-09-02T06:00:55Z`) التقط **16 من الـ61**:

| المؤشر | قبل | بعد |
|---|---|---|
| `exchange_rate_used` | `48.08985` | **`48.2179`** ✅ |
| `usd_price_updated_at` | `2026-08-01` | **`2026-09-02T06:00:55Z`** ✅ |

**طريقة الفحص:** الـ16 مفحوصون واحداً واحداً، و19 عيّنة موزّعة على الـ45 الباقين — كلهم لسّه `48.08985`.

#### 🔑 اكتشاف جديد: سقف الـ40 **مشترك** لا مخصّص

الـ61 كلهم كانوا مؤهّلين، لكن 16 فقط أُخذوا:

```
16 (من الـ61)  +  24 (من الـ29 سجل بتاريخ 2026-08-31، تجاوزوا الـ48 ساعة)  =  40
```

السقف يتقاسمه **كل** المؤهّلين. خذه بالحسبان في أي تقدير مستقبلي للتغطية.

#### متى تكتمل الـ45 الباقية

الكرون `0 6 * * 1-5` — أيام العمل فقط. اليوم **أربعاء**:

| التاريخ | اليوم | متوقّع |
|---|---|---|
| 2026-09-03 | خميس | ~40 |
| 2026-09-04 | جمعة | الباقي (~5) |

**التقدير: تكتمل الجمعة 2026-09-04.** عطلة السبت/الأحد تعني أن أي تأخير يقفز 3 أيام.

> ⚠️ **أثر انتقالي على المطابقة:** الـ45 الباقون محسوبون على صرف `48.08985` بدل `48.2179` — فرق **0.27%**. غير مؤثّر عملياً، لكن نتائج W2 هذه الأيام فيها انحراف طفيف لصالح «أرخص من الواقع».

### تغطية المخزون (429 مشروع)

| المؤشر | القيمة |
|---|---|
| `district_slug` معبّأ | **424/429 (99%)** ✅ |
| سعر USD واحد+ | **121/429 (28%)** 🟠 |

**أكبر المناطق:** basaksehir 46 · beylikduzu 33 · eyupsultan 27 · kagithane 26 · buyukcekmece 24

> **72% من المخزون خارج المطابقة** — NO_MATCH متوقّع بكثرة حتى يكتمل استيراد emlak.

---

## 5. توزيع الفرص (Sales Pipeline — 84 فرصة)

| المرحلة | العدد |
|---|---|
| Lead In | **81** |
| Lost | 2 |
| **Active — Nurture Running** | **1** ← سجل اختبار فقط |
| باقي المراحل | 0 |

**لا يوجد عميل حقيقي في مسار المطابقة.** لم تُرسَل أي رسالة مطابقة (آخر SMS لكونتاكت الاختبار من أيار — من بوت الاستقبال).

---

## 6. الحواجز المتبقّية

| # | الحاجز | الخطورة |
|---|---|---|
| ~~0~~ | ~~**`DRY_RUN: true` غير محسوم**~~ | ✅ **انحلّ 2026-09-03** — لا وجود لأي مفتاح تجريبي |
| 0ب | 🔴 **سباق قراءة الـ10 ثوانٍ** | **مانع نشر** — قد يُقرأ الحقل قبل كتابته → إرسال المشروع السابق |
| ~~0ج~~ | ~~**الفاصلة البادئة → HTTP 400**~~ | ✅ **انحلّ 2026-09-03** — اختبار ضابط: الحقل **فارغ** أعطى 400 أيضاً. الفاصلة بريئة · العقدة ⑥ تبقى كما صُمِّمت |
| 0د | 🔴 **`Send test request` غير صالح للتحقّق** | لا يربط الفرصة → حقول الدمج فارغة. **كل استنتاج بُني عليه اليوم مشكوك فيه**، بما فيه دليل الاستبعاد. التحقّق الوحيد المعتبَر: تشغيل حقيقي |
| 0هـ | 🔴 **العقدة ④ Match Guard لا تُحفظ** | اللوحة تُغلق والعقدة لا تظهر · بلا رسالة خطأ. **البناء متوقف عندها** |
| 1 | **W3 ناقص** (القفل والويبهوك مبنيان · باقي المجموعة لا) | 🔴 المطابقة تحدث بلا إرسال |
| ~~2~~ | ~~**61 سجل بتاريخ 2099**~~ | ✅ **انحلّ ومُثبَت 2026-09-02** — كرون اليوم حدّث 16/61 · الباقي بحلول 09-04 |
| 3 | **بوت الاستقبال غير مربوط** | 🔴 الحارس لن يتوقّف → رعاية لا نهائية |
| 4 | **W1 مكرّر — والنسختان تكمّلان بعضهما** | 🟡 v2 فيها `Property Source` · v3 فيها مسار البيانات الناقصة → **دمج لا حذف** |
| 5 | **W4/W5/W6 غير موجودة** | 🟡 لا معالجة رد ولا تحقّق توفّر |
| 6 | **تغطية 28%** | 🟠 NO_MATCH متكرّر |
| ~~7~~ | ~~**W2 منشور بلا اختبار كامل**~~ | ✅ **انحلّ 2026-09-02** — أُعيد إلى Draft |

### حواجز انحلّت هذه الجلسة ✅
- ~~تأكيد الكرون~~ → مؤكّد شغّال
- ~~إثبات MATCHED حقيقي~~ → مثبّت
- ~~حسم حالة W2~~ → `published` مؤكّد
- ~~صلاحية الـMCP~~ → تعمل
- ~~W2 منشور~~ → أُعيد إلى Draft ✅
- ~~حقل `nurture_active` مفقود~~ → أُنشئ ومُثبَت ✅
- ~~خلل الـ61 (2099)~~ → أُصلح ومُثبَت بالكرون ✅

---

## 7. الترتيب المقترح

| # | الخطوة | الوقت | من ينفّذ |
|---|---|---|---|
| ~~1~~ | ~~**صفّر الـ61 سجل بتاريخ 2099**~~ | ✅ | **مُنجز ومُثبَت** — كُتب `2026-08-01` · 61/61 · كرون 09-02 التقط 16 منهم |
| ~~1ب~~ | ~~**أنشئ حقل `nurture_active`**~~ | ✅ | **مُنجز 2026-09-02** — `PoxnDg8w2iJq4NrC2eLB` · RADIO Yes/No |
| ~~0~~ | ~~**افحص `DRY_RUN`**~~ | ✅ | **مُنجز 2026-09-03** — لا مفتاح تجريبي · وأُضيف المفتاحان الناقصان لـW3 |
| ~~0ب~~ | ~~**اختبر الفاصلة البادئة**~~ | ✅ | **مُنجز 2026-09-03** — النتيجة: `400 Bad Request` · عطل مؤكّد |
| ~~0ب2~~ | ~~**أصلح المحرّك**~~ | ✅ | **ملغى** — الكود سليم أصلاً (`filter(Boolean)` موجود) |
| **0ب3** | 🔴 **افحص RAW BODY في W3** — هل `{{opportunity.sent_project_ids}}` بين `" "`؟ | **1 د** | برومت |
| ~~0ج~~ | ~~**فرّغ `Sent Project IDs`**~~ | ✅ | **مُنجز 2026-09-03** — مؤكّد بالـAPI |
| 2 | **ادمج نسختَي W1** (لا تحذف — كل واحدة فيها منطق مفقود بالأخرى) | 30 د | أنت — يدوي |
| 3 | **ابنِ W3** حسب الدليل | **5–6 س** (≈76 عقدة) | أنت — يدوي |
| 4 | **اختبر W3** بانتظارات قصيرة | 30 د | أنت + أنا |
| 5 | **اربط بوت الاستقبال** بنقل المرحلة | 1 س | أنت |
| 6 | انشر W1 ثم W3 بعد الاختبار | 10 د | أنت |
| 7 | ابنِ W5 ثم W6 | — | لاحقاً |
| 8 | **استيراد emlak** (رفع التغطية) | — | بالتوازي |

---

## 8. تعارضات في التوثيق (تحتاج حسماً)

| # | التعارض | الأصح |
|---|---|---|
| 1 | Blueprint: «أرخص **2**» · W3 spec: «خيار **واحد**» | **خيار واحد** — القرار الأحدث. الـBlueprint لم يُحدَّث |
| 2 | منطق الجنسية (خلط الأنواع لسقف 400k) | محسوم بالتصميم · **غير مبني بالكود** |
| 3 | حلقة التحقق 2 الدورية | موصوفة بالـBlueprint · **غير موجودة في أي W** |
| 4 | kill-switch | مشترط قبل النشر · **غير مبني** |

---

## 9. ملاحظات تقنية

**الوصول للحساب:** توكن ماجد (`pit-6929…`) يعمل مع `locationId=jo8GkEPOGeRVWN5khioH`.
الصلاحيات المتاحة: `workflows.readonly` · `opportunities.readonly` · `contacts.readonly` · `locations/customFields.readonly` · **`objects/record.write` (كتابة — مثبتة عملياً 2026-09-02)**.

> **تصحيح:** التوثيق السابق قال إن التوكن قراءة فقط. **غير صحيح** — الكتابة على سجلات Projects تعمل (`update-object-record` → 200). القيد الحقيقي محصور بالـWorkflows فقط.
>
> ⚠️ **`dryRun` لا يفحص الصلاحيات** — يُرجع `success: true` مع `authorizationVerified: false`. الفحص الصحيح: كتابة عديمة الأثر (نفس القيمة الحالية).

**⚠️ قيد ثانٍ — وحدة الـWorkflows ترفض الإدخال البرمجي (مثبَت 2026-09-02):**

جدول الـWorkflows داخل إطار محصّن: **لا يقبل كتابة في البحث، ولا ضغطاً على الصفوف أو المجلدات.** فُحص على:

| المسار | النتيجة |
|---|---|
| `app.gohighlevel.com` بالمتصفح | ❌ |
| `app.innovationvalues.com` (الدومين الأبيض) بالمتصفح | ❌ |
| تطبيق innova بأدوات الشاشة | ❌ كل الضغطات مرفوضة |
| تطبيق innova بأدوات الخلفية (`app_*`) | ❌ محتوى الـwebview لا يُلتقط |
| روابط مباشرة لأي صفحة عميقة | ❌ صفحة بيضاء (تحميل كامل يكسر الـSPA) |

> **صفحة Custom Fields تعمل بالمقابل** — أُنشئ فيها `nurture_active` برمجياً بنجاح. **القيد على وحدة الـWorkflows تحديداً، لا على الحساب ولا الدومين.**
>
> **الأثر:** بناء الـWorkflows يحتاج إيداً بشرية أو أداة أخرى (ChatGPT/Codex). التحقق والقراءة والحقول تبقى ممكنة برمجياً.

**⚠️ قيد جوهري — GHL API لا يكتب Workflows:**

| العملية | متاحة؟ |
|---|---|
| قراءة اللائحة | ✅ |
| قراءة workflow تفصيلياً | ❌ 404 |
| إنشاء / تعديل / حذف | ❌ لا endpoint |
| نشر / تحويل لـDraft | ❌ لا endpoint |

**كل عمل على الـWorkflows يدوي بالواجهة.** لا مفرّ.

---

## 10. خريطة الملفات

| الملف | المحتوى |
|---|---|
| **`review/PROJECT_STATUS.md`** | 📍 **هذا الملف — ابدأ منه** |
| `review/W3_BUILD_GUIDE.md` | دليل بناء W3 التنفيذي |
| **`review/W3_GUIDE_REVIEW.md`** | ⚠️ **مراجعة الدليل — اقرأها قبل البناء** (6 مشاكل، أهمها فروع If/Else لا تلتقي) |
| `review/ACTION_W1_W2.md` | إجراءا W1/W2 اليدويان |
| `review/REVIEW_2026-09-01.md` | المراجعة الأصلية + التعارضات |
| `review/NEXT_SESSION_CHECKS.md` | سجل رحلة إصلاح MCP |
| `claude/majed-system-blueprint.md` | المخطط المرجعي |
| `claude/majed-w3-spec.md` | مواصفات W3 المثبّتة |
| `claude/majed-build-log.md` | سجل البناء (محدَّث بهذه الجلسة) |
| `claude/majed-clean-schema.md` | مخطط الحقول |
| `review/prompts/EXECUTE_ALL_PROMPT.md` | برومت التنفيذ الكامل (لـChatGPT/Codex) |
| `review/prompts/CONTINUE_W3_PROMPT.md` | برومت رفع مانع الكاش |
| `review/prompts/ASK_AI_PROBE.md` | برومت فحص قدرة Ask AI + تشخيص `DRY_RUN` (⚠️ منتهٍ — حُسم) |
| `review/prompts/DRY_RUN_PROBE.md` | برومت الفحص المركّز الذي حسم `DRY_RUN` (مرجع لصياغة برومتات لاحقة) |

---

*Innova Smart Solutions — حالة مشروع ماجد كرف · 2026-09-02*
