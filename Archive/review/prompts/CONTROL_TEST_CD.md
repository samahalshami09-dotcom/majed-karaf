# برومت 1 — اختبار C/D الضابط (ابدأ بهذا)

**الصق كل ما تحت الخط في ChatGPT.**

---

مهمة — اختباران متتاليان على عقدة واحدة. **لا تخرج من العقدة بينهما.**

## ⛔ حدود
- ممنوع تعديل RAW BODY أو التوكن أو أي إعداد للعقدة
- ممنوع نشر W3 — يبقى Draft · لا SMS
- ممنوع ذكر أي جزء من رمز التفويض
- ممنوع الروابط المباشرة لصفحات GHL العميقة — التنقّل بالضغط فقط

## 🔑 القاعدة الأهم
**لا تضغط Cancel ولا تغادر عقدة الويبهوك بين الاختبارين.** افتحها مرة واحدة وأبقِها مفتوحة.
وقبل كل `Send test request`: **تأكّد بصرياً أن سجل الاختبار ما زال مختاراً في اللوحة**
(Sema MatchTest · الفرصة `63oh6ELZtHHN6AUsY2jX`). إن كان فارغاً أو تغيّر — أعد اختياره **وقل ذلك في ردّك**.

> هذه النقطة هي جوهر الاختبار كله. الفرضية المطروحة أن اللوحة تفقد ارتباطها بالسجل عند إعادة الفتح، فتُحلّ حقول الدمج فارغة ويفشل الطلب لسبب لا علاقة له بما نختبره.

## التنقّل
`app.innovationvalues.com` → Sub-Accounts → ابحث `majed` → **Majed Karaf** → Switch
Automation → Workflows → `W3 — Options Delivery & Nurture`
**ID `5e7af32d-5378-42ef-bdb2-f95daf559e60`** — تأكّد منه
→ عقدة **Custom Webhook**

## اختبار C — الضابط (الحقل فارغ)
1. تأكّد أن `Sent Project IDs` على الفرصة `63oh6ELZtHHN6AUsY2jX` **فارغ**
2. أكّد أن السجل مختار في اللوحة
3. `Send test request` → سجّل Status Code و Response Body كاملاً

## اختبار D — المتغيّر (فاصلة بادئة)
4. **بلا إغلاق العقدة:** اكتب في `Sent Project IDs` = `,6a95ca3749b8afb35f2a13ca` واحفظ
5. ارجع للعقدة **المفتوحة**، أكّد أن السجل ما زال مختاراً
6. `Send test request` → سجّل Status Code و Response Body كاملاً
7. امسح `Sent Project IDs` · احفظ · أكّد بصرياً أنه فارغ

## صيغة الرد — التزم بها حرفياً
⛔ لا تسرد خطواتك. أعطني هذا فقط:

```
── اختبار C (فارغ) ──
السجل كان مختاراً؟ : نعم / لا — واحتجت إعادة اختياره؟
Status Code        : ...
Response Body      : (كاملاً وحرفياً)

── اختبار D (فاصلة بادئة) ──
السجل كان مختاراً؟ : نعم / لا — واحتجت إعادة اختياره؟
Status Code        : ...
Response Body      : (كاملاً وحرفياً)

── تأكيدات ──
هل أغلقت العقدة بين الاختبارين؟ : نعم / لا
الحقل فُرِّغ في النهاية          : نعم / لا
W3 لا يزال Draft                : نعم / لا
أي SMS أُرسل                    : نعم / لا
```

إن فشلت في أي خطوة — قف وقل عند أيها وما الذي رفض. لا تكمل ولا تلتفّ.
